when:
  - event: push
  - event: manual
steps:
  process:
    image: node:26.7.0
    commands:
      - npm ci
      - npm run check
      - npm run formatcheck
      - npm test
      - npm run pack:check
