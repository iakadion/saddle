# buckets/sql_thirdparty/ - SQL gratuito de terceiros

# Turso (SQLite edge): free 9GB storage, 500 databases, 500M rows read
# turso db create opencode-storage --group default
# turso db shell opencode-storage "CREATE TABLE dumps (id TEXT, data BLOB);"

# Neon (Postgres serverless): free 0.5GB storage, 1 project, compute 100h
# Neon escala pra zero, só cobra quando usa

# Supabase (Postgres): free 500MB DB, 1GB storage, 2GB bandwidth
# Supabase Storage: S3 compatível, free 1GB

# Drizzle ORM + Better SQLite3 local que synca pra Turso
# import { drizzle } from 'drizzle-orm/better-sqlite3'
# import Database from 'better-sqlite3'
# const sqlite = new Database('opencode.db')
# const db = drizzle(sqlite)

# Prisma com MySQL2
# datasource db { provider = "mysql" url = env("DATABASE_URL") }

# Ideia: guarda binário como base64 em BLOB no Turso/Neon, serve via Pages API

# Exemplo: usa Turso como queue de jobs para farm multi-repo
# INSERT INTO jobs (id, status, result_url) VALUES ('job-123', 'queued', '')
# Worker Action lê, processa, UPDATE status='done'
